export interface Property {
    id: number;
    title: string;
    price: number;
    location: string;
    imageUrl: string;
    description: string;
}

export interface User {
    email: string;
    name: string;
}